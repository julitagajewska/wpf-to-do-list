<UserControl x:Class="ToDoListApp.MVVM.View.EditTaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoListApp.MVVM.View"
        xmlns:mvvm="clr-namespace:ToDoListApp.MVVM.ViewModel"
        mc:Ignorable="d" 
        d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedTask.Name, Mode=TwoWay}"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedTask.Description, Mode=TwoWay}"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Deadline:"/>
        <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding SelectedTask.Deadline, Mode=TwoWay}"/>
        <TextBlock Grid.Row="3" Grid.Column="0" Text="StartDate:"/>
        <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding SelectedTask.StartDate, Mode=TwoWay}"/>
        <TextBlock Grid.Row="4" Grid.Column="0" Text="EndDate:"/>
        <DatePicker Grid.Row="4" Grid.Column="1" SelectedDate="{Binding SelectedTask.EndDate, Mode=TwoWay}"/>
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Status:"/>
        <ComboBox Grid.Row="5" Grid.Column="1" ItemsSource="{Binding statuses}" SelectedItem="{Binding SelectedTask.Status, Mode=TwoWay}"></ComboBox>
        <TextBlock Grid.Row="6" Grid.Column="0" Text="Priority:"/>
        <ComboBox Grid.Row="6" Grid.Column="1" ItemsSource="{Binding priorities}" SelectedItem="{Binding SelectedTask.Priority, Mode=TwoWay}"></ComboBox>
        <TextBlock Grid.Row="7" Grid.Column="0" Text="Categories:"/>
        <ListBox x:Name="ListBoxCategories" Grid.Row="7" Grid.Column="1" SelectionMode="Extended"
         ItemsSource="{Binding TaskCategories}" SelectedItem="{Binding ListBoxSelectedItems, Mode=TwoWay}"
         IsEnabled="{Binding IsListBoxEnabled}" Loaded="ListBoxCategories_Loaded">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" Content="Save" Command="{Binding EditTaskCommand}"/>
    </Grid>
</UserControl>
